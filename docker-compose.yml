services:
  api:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "3030:3030"
    restart: unless-stopped
