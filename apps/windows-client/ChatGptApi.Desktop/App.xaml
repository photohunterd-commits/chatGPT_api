<Application x:Class="ChatGptApi.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#05070A" />
        <SolidColorBrush x:Key="ShellBrush" Color="#0E1216" />
        <SolidColorBrush x:Key="PanelBrush" Color="#12171B" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#181D22" />
        <SolidColorBrush x:Key="PanelMutedBrush" Color="#090D10" />
        <SolidColorBrush x:Key="PanelGlassBrush" Color="#CC151B20" />
        <SolidColorBrush x:Key="PanelHighBrush" Color="#242A30" />
        <SolidColorBrush x:Key="SidebarActiveBrush" Color="#0D1B1A" />
        <SolidColorBrush x:Key="SidebarHoverBrush" Color="#13191D" />
        <SolidColorBrush x:Key="GhostBorderBrush" Color="#263238" />
        <SolidColorBrush x:Key="BorderBrush" Color="#1C252B" />
        <SolidColorBrush x:Key="AccentBrush" Color="#7AE7D8" />
        <SolidColorBrush x:Key="AccentMutedBrush" Color="#12322E" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#29A195" />
        <SolidColorBrush x:Key="AccentDeepBrush" Color="#0E615A" />
        <SolidColorBrush x:Key="DiamondMintBrush" Color="#8FFCF0" />
        <SolidColorBrush x:Key="DiamondBrightBrush" Color="#B8FFF7" />
        <SolidColorBrush x:Key="DiamondDeepBrush" Color="#0C5E53" />
        <SolidColorBrush x:Key="TextBrush" Color="#F0F4F7" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#BCC9C6" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#708087" />
        <SolidColorBrush x:Key="WarningBrush" Color="#F5C06A" />
        <SolidColorBrush x:Key="ScrollTrackBrush" Color="#111518" />
        <SolidColorBrush x:Key="ScrollThumbBrush" Color="#2E4346" />
        <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#3D5E62" />
        <SolidColorBrush x:Key="ScrollThumbPressedBrush" Color="#4F7B7E" />
        <LinearGradientBrush x:Key="PrimaryGradientBrush"
                             StartPoint="0,0"
                             EndPoint="1,1">
            <GradientStop Offset="0" Color="#7AE7D8" />
            <GradientStop Offset="1" Color="#29A195" />
        </LinearGradientBrush>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Geometry x:Key="ProjectIconGeometry">F1 M4,8 C4,6.89543 4.89543,6 6,6 L10.6,6 L12.2,8 L18,8 C19.1046,8 20,8.89543 20,10 L20,17 C20,18.1046 19.1046,19 18,19 L6,19 C4.89543,19 4,18.1046 4,17 Z</Geometry>
        <Geometry x:Key="ChatIconGeometry">F1 M6,5 L18,5 C19.1046,5 20,5.89543 20,7 L20,14 C20,15.1046 19.1046,16 18,16 L11,16 L7,19.5 L7,16 L6,16 C4.89543,16 4,15.1046 4,14 L4,7 C4,5.89543 4.89543,5 6,5 Z</Geometry>
        <Geometry x:Key="UserIconGeometry">F1 M12,12 C14.7614,12 17,9.76142 17,7 C17,4.23858 14.7614,2 12,2 C9.23858,2 7,4.23858 7,7 C7,9.76142 9.23858,12 12,12 M4.5,21 C5.44698,17.5204 8.39532,15 12,15 C15.6047,15 18.553,17.5204 19.5,21</Geometry>
        <Geometry x:Key="BudgetIconGeometry">F1 M4,7 C4,5.34315 7.58172,4 12,4 C16.4183,4 20,5.34315 20,7 C20,8.65685 16.4183,10 12,10 C7.58172,10 4,8.65685 4,7 Z M4,12 C4,13.6569 7.58172,15 12,15 C16.4183,15 20,13.6569 20,12 M4,17 C4,18.6569 7.58172,20 12,20 C16.4183,20 20,18.6569 20,17</Geometry>
        <Geometry x:Key="SettingsIconGeometry">F1 M12,8.25 C9.92893,8.25 8.25,9.92893 8.25,12 C8.25,14.0711 9.92893,15.75 12,15.75 C14.0711,15.75 15.75,14.0711 15.75,12 C15.75,9.92893 14.0711,8.25 12,8.25 M12,3.5 L13.05,5.65 C13.4218,5.74729 13.7803,5.88636 14.118,6.063 L16.35,5.35 L18.1,7.1 L17.387,9.332 C17.5636,9.66974 17.7027,10.0282 17.8,10.4 L19.95,11.45 L19.95,12.55 L17.8,13.6 C17.7027,13.9718 17.5636,14.3303 17.387,14.668 L18.1,16.9 L16.35,18.65 L14.118,17.937 C13.7803,18.1136 13.4218,18.2527 13.05,18.35 L12,20.5 L10.9,20.5 L9.85,18.35 C9.47821,18.2527 9.11974,18.1136 8.782,17.937 L6.55,18.65 L4.8,16.9 L5.513,14.668 C5.33636,14.3303 5.19729,13.9718 5.1,13.6 L2.95,12.55 L2.95,11.45 L5.1,10.4 C5.19729,10.0282 5.33636,9.66974 5.513,9.332 L4.8,7.1 L6.55,5.35 L8.782,6.063 C9.11974,5.88636 9.47821,5.74729 9.85,5.65 L10.9,3.5 Z</Geometry>
        <Geometry x:Key="RefreshIconGeometry">F1 M20,6 L20,12 L14,12 M19,13 C18.3807,16.9923 14.9263,20 10.75,20 C6.13462,20 2.25,16.1387 2.25,11.5 C2.25,6.86129 6.13462,3 10.75,3 C13.8885,3 16.657,4.76213 18.073,7.371</Geometry>
        <Geometry x:Key="SendIconGeometry">F1 M3,11.5 L21,3 L16,21 L11.4,13.6 Z M11.4,13.6 L21,3</Geometry>
        <Geometry x:Key="WarningIconGeometry">F1 M12,4 L20,19 L4,19 Z M12,9 L12,13.2 M12,16.2 L12,16.4</Geometry>
        <Geometry x:Key="PlusIconGeometry">F1 M12,5 L12,19 M5,12 L19,12</Geometry>
        <Style x:Key="VectorIconStyle" TargetType="Path">
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Stretch" Value="Uniform" />
            <Setter Property="StrokeThickness" Value="1.8" />
            <Setter Property="Stroke" Value="{StaticResource SubtleTextBrush}" />
            <Setter Property="StrokeStartLineCap" Value="Round" />
            <Setter Property="StrokeEndLineCap" Value="Round" />
            <Setter Property="StrokeLineJoin" Value="Round" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="AccentVectorIconStyle"
               TargetType="Path"
               BasedOn="{StaticResource VectorIconStyle}">
            <Setter Property="Stroke" Value="{StaticResource AccentBrush}" />
        </Style>
        <Style x:Key="WarningVectorIconStyle"
               TargetType="Path"
               BasedOn="{StaticResource VectorIconStyle}">
            <Setter Property="Stroke" Value="{StaticResource WarningBrush}" />
        </Style>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style x:Key="ScrollBarPageButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="MinWidth" Value="44" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Margin="2"
                                Background="{StaticResource ScrollThumbBrush}"
                                CornerRadius="999" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Background"
                                        Value="{StaticResource ScrollThumbHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Background"
                                        Value="{StaticResource ScrollThumbPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
            <Setter Property="MinWidth" Value="14" />
            <Setter Property="MinHeight" Value="14" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Border Margin="4,2"
                                    Background="{StaticResource ScrollTrackBrush}"
                                    CornerRadius="999" />
                            <Track x:Name="PART_Track"
                                   Margin="2"
                                   IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="{x:Static ScrollBar.PageUpCommand}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="{x:Static ScrollBar.PageDownCommand}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="PART_Track"
                                        Property="IsDirectionReversed"
                                        Value="False" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="14" />
                    <Setter Property="MinHeight" Value="14" />
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="Transparent">
                                    <Border Margin="2,4"
                                            Background="{StaticResource ScrollTrackBrush}"
                                            CornerRadius="999" />
                                    <Track x:Name="PART_Track"
                                           Margin="2"
                                           IsDirectionReversed="False">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                          Command="{x:Static ScrollBar.PageLeftCommand}" />
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                          Command="{x:Static ScrollBar.PageRightCommand}" />
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelMutedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TextBoxBorder"
                                        Property="BorderBrush"
                                        Value="#335EE3D7" />
                                <Setter TargetName="TextBoxBorder"
                                        Property="Background"
                                        Value="{StaticResource PanelBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelMutedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="PasswordBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="PasswordBorder"
                                        Property="BorderBrush"
                                        Value="#335EE3D7" />
                                <Setter TargetName="PasswordBorder"
                                        Property="Background"
                                        Value="{StaticResource PanelBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RichTextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#042B29" />
            <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="18,11" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.96" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.84" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="GhostButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="ChipBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Background" Value="{StaticResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource GhostBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="IconTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
